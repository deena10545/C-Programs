#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num;
    printf("Enter a number to get square and cube:");
    scanf("%d",&num);
    printf("square of num: %d\n", num*num);
    printf("Cube of num: %d", num*num*num);
}
